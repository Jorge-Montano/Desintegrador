# Desintegración particular 3D

## Setup

Make sure to install dependencies:

```Modules
# Ecosistema 3D y animaciones
npm install three @tresjs/core @tresjs/cientos gsap

# Tipos
npm install -D @types/three

```
